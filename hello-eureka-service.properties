server.port=8761

#By default, the registry also tries to register itself, so you need to disable that behavior as well.
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
eureka.instance.lease-renewal-interval-in-seconds=30
# If not disabled,When the registry starts, it will complain (with a stacktrace) that 
#there are no replica nodes to which the registry can connect.
logging.level.com.netflix.eureka=OFF
logging.level.com.netflix.discovery=OFF
